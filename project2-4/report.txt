크기가 n인 해시테이블을 만든다.
나눗셈법을 사용하여 key를 삽입한다.
만약 노드가 차있을 경우 line probing을 통해 빈 노드를 찾는다.
빈 노드와 원래 위치의 거리가 sizeof(key)보다 클 경우 빈 노드에서 Hash(sizeof(key)-1)를 확인해 노드가 차있으면 빈노드로 옮긴다.
원래 위치의 노드가 비워질때까지 값을 옮겨 저장
해시함수 return값의 위치에서 거리가 sizeof(key) 이내인 곳에 key가 없으면 해시테이블에 key가 없는 것이므로
for문을 돌려서 값을 확인
